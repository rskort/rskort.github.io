title: "rskort"
url: "https://rskort.github.io"
theme: null
markdown: kramdown
kramdown:
  input: GFM

# Every Markdown file uses the page layout by default
defaults:
  - scope:
      path: ""
    values:
      layout: "page"
